.. -*- coding: utf-8; mode: rst -*-
.. src-file: drivers/usb/mtu3/mtu3_core.c

.. _`mtu3_set_dma_mask`:

mtu3_set_dma_mask
=================

.. c:function:: int mtu3_set_dma_mask(struct mtu3 *mtu)

    bit DMA mask by default, here check whether the controller supports 36-bit DMA or not, if it does, set 36-bit DMA mask.

    :param mtu:
        *undescribed*
    :type mtu: struct mtu3 \*

.. This file was automatic generated / don't edit.

